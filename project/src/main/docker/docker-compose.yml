version: '2'

services:
  event_manager:
    image: postgres
    container_name: event_manager
    restart: always
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: postgres
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: admin123